"""
OCRLLM 提示词模板 — 所有 LLM 提示词集中管理。

每个模板使用 str.format() 占位符，调用处负责填充。

所有元数据（页码、帧编号、时间等）使用 HTML 注释标记，格式为：
  <!-- meta:TYPE key=value ... -->
这样在 Markdown 渲染时不可见，但方便程序解析。
"""

PDF_FORMULA = """这是带有数学公式和表格的课本/课件的第{page_range}页。
请严格转写图片中的全部内容，并输出为可直接预览的 Markdown。

硬性格式要求：
- 数学公式一律使用 LaTeX 表达，不要用 Unicode 上下标、Unicode 分数、Unicode 求和/积分符号、Unicode 数学字母去代替 LaTeX 公式。
- 行内公式使用 $...$，独立公式使用 $$...$$。
- 正文中如果出现变量、下标、上标、分式、矩阵、求和、积分、极限、概率分布、希腊字母等数学表达，优先写成 LaTeX，而不是类似 x₁、x²、∑、∫、α 这样的 Unicode 形式。
- 表格必须使用标准 Markdown 表格语法（| 列1 | 列2 |），绝对禁止用 ``` 代码块包裹表格。
- 严禁输出 ```markdown 或 ```md。严禁用 ``` 包裹正文、标题、列表、公式或表格。
- ``` 只允许用于原文中出现的真实代码片段。
- 每页之前必须且只能有一行 HTML 注释作为页标记，格式严格为 <!-- meta:page number=X -->，X 必须是实际页码，不能重号，不能回退。
- 除页标记注释外，正文中使用的标题一律从 ## 开始，禁止出现 # 一级标题。

内容要求：
- 不添加额外内容
- 不遗漏内容
- 不简并内容
- 保留分栏结构
- 不需要输出任何图片中没有出现的内容

只输出最终 Markdown，不要输出任何解释、说明或额外前后缀。"""

BOARD = """这是上课板书/课件截图（图片编号：{image_names}）。
这些图片可能来自黑板、白板或投影PPT，可能具有手写个人风格。
请你在保留数学公式（使用LaTeX格式）和表格结构（使用Markdown表格）的情况下，识别并输出Markdown文件。
要求：
- 不添加额外内容，不遗漏内容，不简并内容
- 每张图片之前必须有一行 HTML 注释作为帧标记，格式为 <!-- meta:frame id=图片名 -->，然后是正文内容
- 识别手写文字时尽量准确，对于不确定的字符请保留最可能的结果
- 数学公式使用 LaTeX：行内 $...$，独立 $$...$$
- 表格必须使用标准 Markdown 表格语法，禁止用 ``` 代码块包裹表格
- ``` 只允许用于原文中出现的真实代码片段
- 正文中使用的标题一律从 ## 开始，禁止出现 # 一级标题
不需要输出任何图片中没有出现的内容。"""

BOARD_WITH_HOTWORDS = """这是上课板书/课件截图（图片编号：{image_names}）。
图片带有时间坐标信息。这些图片可能来自黑板、白板或投影PPT，可能具有手写个人风格。
请你在保留数学公式（使用LaTeX格式）和表格结构（使用Markdown表格）的情况下，识别并输出Markdown文件。
要求：
- 不添加额外内容，不遗漏内容，不简并内容
- 每张图片之前必须有一行 HTML 注释作为帧标记，然后是正文内容。格式示例：<!-- meta:frame id=board_003_504s time=08:24 -->（id 和 time 从图片编号中的名称和时间获取）
- 识别手写文字时尽量准确，对于不确定的字符请保留最可能的结果
- 数学公式使用 LaTeX：行内 $...$，独立 $$...$$
- 表格必须使用标准 Markdown 表格语法，禁止用 ``` 代码块包裹表格
- ``` 只允许用于原文中出现的真实代码片段
- 正文中使用的标题一律从 ## 开始，禁止出现 # 一级标题
不需要输出任何图片中没有出现的内容。

{extra_instruction}"""

EXTRACT_HOTWORDS = """基于你刚才识别的所有板书内容，请提取一份热词表。
这些热词是语音识别可能出错的专业术语，包括但不限于：
- 希腊字母（如 sigma, alpha, beta 等）
- 数学符号的口语表达（如"积分"、"偏导"、"矩阵"、"行列式"等）
- 课程专有名词
- 其他日常对话中不常出现的学术用语

请以如下格式输出，每行一个热词：
热词1
热词2
热词3
..."""

EXTRACT_HOTWORDS_FROM_FRAMES = """请仔细观察这些视频截图，提取画面中出现的**关键专业术语**。
这些术语将用于辅助语音识别（ASR），帮助 ASR 正确识别视频中口述的专有名词。

只提取以下类型：
1. 英文专有名词：项目名、产品名、品牌名（如 GitHub, Figma, Vercel, UIverse）
2. 技术缩写和专业术语（如 API, SDK, npm, Tailwind CSS, OpenType）
3. 域名和 URL 关键词（如 github.com, vercel.app）
4. 不常见的人名、公司名、仓库名（如 VoltAgent, awesome-design-md）

不要提取：
- 通用 UI 文字（Search, Filter, Submit, Cancel, View, Export, Star, Fork 等）
- 极常见英文词（design, code, class, action, status, online, profile 等）
- 编程语言名称的简单罗列（除非该语言是视频核心主题）
- 超过 4 个单词的长短语或整句
- 纯中文常见词（"按钮"、"卡片"、"导航"、"搜索"等）
- 纯数字

要求：
- 从画面可见文字中提取，保持原始拼写
- 每行一个热词，不编号，不解释，不重复
- 最多输出 30 个最关键的术语
- 只输出热词列表"""

AUDIO_TRANSCRIBE = """请把这段课程录音尽量逐字转写成中文文本。
要求：
- 只输出转写内容本身，不要解释，不要总结，不要补充前言或后记
- 数学符号、公式、英文术语按最接近原始讲述和课程语境的方式写出
- 不要因为语句不完整就擅自重写或润色
- 如果听到重复、停顿或口误，以实际听到的内容为准

{hotwords_instruction}"""

SHORT_VIDEO_RECOGNIZE = """这是来自社交媒体的知识类短视频截图（场景 {scene_id}，时间 {time_range}）。
图片编号：{image_names}。

请仔细观察并详细描述你所看到的全部画面内容，输出为可直接预览的 Markdown。

内容要求：
- 详细描述画面中的所有视觉元素：图像、动画、图表、示意图、人物动作等
- 精确转写画面中出现的所有文字，包括标题、正文、字幕条、水印文字等
- 如果画面中有字幕（硬字幕或软字幕），请准确记录字幕内容
- 如果画面内容涉及公式或数学表达，使用 LaTeX 格式（行内 $...$，独立 $$...$$）
- 如果画面中有表格，使用标准 Markdown 表格语法
- 保留画面中信息的层次和结构关系

格式要求：
- 每组图片之前必须有一行 HTML 注释作为场景标记：<!-- meta:scene id={scene_id} time={time_range} -->
- 正文标题一律从 ## 开始，禁止出现 # 一级标题
- 禁止输出 ```markdown 或 ```md，禁止用 ``` 包裹正文
- ``` 只允许用于真实代码片段

只输出最终 Markdown，不要输出解释或额外前后缀。"""
